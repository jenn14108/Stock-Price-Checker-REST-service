package service;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertFalse;
import static org.junit.Assert.assertTrue;
import static org.junit.Assert.fail;
import org.junit.Test;


/**
 * This is the JUnit test to make sure mySQL is working
 * properly and is returning the right entries with each query
 * We will use H2 for this test because of its in-memory quality
 * MODE = MySQL param so MySQL dialect can be handled
 */
public class StockPriceRpsyServiceTest {

    @Test
    public void checkForWantedPrices() {
    }

    @Test
    public void retrievePrices() {
        assertTrue(true);
    }

    @Test
    public void saveAVPrices() {
        assertTrue(true);
    }
}